apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  labels:
    app: inven
  name: inven
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inven
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:        
        app: inven        
    spec:
      containers:
      - image: ezdehar/book-info-inv-hq:0.0.2-SNAPSHOT
        imagePullPolicy: IfNotPresent
        name: inven-1  
        resources:
          requests:
            cpu: "60m"
            memory: "768Mi"
          limits:
            cpu: "100m"
            memory: "1024Mi"       
---
apiVersion: v1
kind: Service
metadata:
  labels:
    job: inven  
    app: inven
  name: inven
  annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8101" 
        prometheus.io/path: "/actuator/prometheus"
  namespace: default
spec:
  ports:
  - port: 8101
    name: metrics
    protocol: TCP
    targetPort: 8101
  selector:
    app: inven
  sessionAffinity: None
  type: ClusterIP

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: inv-sm
  labels:
    release: prometheus-operator
    app: prometheus-operator
spec:
  jobLabel: job
  selector:
    matchLabels:
      app: inven
  endpoints:
  - port: metrics
    path: /actuator/prometheus
    interval: 30s
