apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  labels:
    app: inven2
  name: inven2
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inven2
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:        
        app: inven2        
    spec:
      containers:
      - image: ezdehar/book-info-inv1-lq:0.0.2-SNAPSHOT
        imagePullPolicy: IfNotPresent
        name: inven2-1    
        resources:
          requests:
            cpu: "60m"
            memory: "768Mi"
          limits:
            cpu: "100m"
            memory: "1024Mi"     
---
apiVersion: v1
kind: Service
metadata:
  labels:
    job: inven2  
    app: inven2
  name: inven2
  annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8100" 
        prometheus.io/path: "/actuator/prometheus"
  namespace: default
spec:
  ports:
  - port: 8100
    name: metrics
    protocol: TCP
    targetPort: 8100
  selector:
    app: inven2
  sessionAffinity: None
  type: ClusterIP

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: inv2-sm
  labels:
    release: prometheus-operator
    app: prometheus-operator
spec:
  jobLabel: job
  selector:
    matchLabels:
      app: inven2
  endpoints:
  - port: metrics
    path: /actuator/prometheus
    interval: 30s
